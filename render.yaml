services:
  - type: web
    name: hft-engine
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /
    buildCommand: docker build -t hft-engine .
    startCommand: docker run -p 8000:8000 hft-engine 