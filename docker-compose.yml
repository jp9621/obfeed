version: '3.8'

services:
  market-feed:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: market-feed
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    # Healthcheck is optional - service will be ready when port 8000 is listening
    restart: unless-stopped
